// Exemplo de JavaScript simples
document.addEventListener("DOMContentLoaded", function () {
    // Este código será executado quando a página estiver completamente carregada
    // Você pode adicionar suas interações JavaScript aqui

    // Por exemplo, aqui estamos alterando o texto de um elemento com id "exemplo"
    var exemploElemento = document.getElementById("exemplo");
    if (exemploElemento) {
        exemploElemento.textContent = "Este texto foi alterado por JavaScript!";
    }
});
